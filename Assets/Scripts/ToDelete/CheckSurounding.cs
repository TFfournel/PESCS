using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CheckSurounding: MonoBehaviour
{
    private Collider collider;
    private List<GameObject> allDetectedObject = new List<GameObject>();

    private void Start()

    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
}